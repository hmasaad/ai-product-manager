# Optional. Without a key, the planner still runs from the brief.
# It will not invent metrics, competitors, or a solution the source did not state.
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.6-flash

# Used only when GEMINI_API_KEY is empty.
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o
